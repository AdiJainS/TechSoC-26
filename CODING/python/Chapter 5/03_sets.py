e = set() # dont use d = { } it will reate empty dict.
s = {1,2,3,4,1,2}
#s.add(6)
print(len(s))
#s.remove(1)
#item = s.pop() ## Randomly removes and prints an item
#item = s.clear()
print(s,type (s))
