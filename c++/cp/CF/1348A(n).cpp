#include <bits/stdc++.h>
#include <cmath>
using namespace std;

int main() {
    int t;
    cin >> t;  // number of test cases
    while (t--) {
        int n;
        cin >> n;
          if (n==2)  cout << (int)pow(2, n)-(int)pow(2,n-1) << "\n" ;
          
            
            

        }
    }

